class Serie {
    constructor(codigo, nome, data_lancamento, qtd_temporadas, diretor) {
        this.codigo = codigo;
        this.nome = nome;
        this.data_lancamento = data_lancamento;
        this.qtd_temporadas = qtd_temporadas;
        this.diretor = diretor;
    }
}

module.exports = Serie; 